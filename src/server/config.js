const dotenv = require('dotenv');
dotenv.config();
module.exports.KEY = {
    apiKey: process.env.API_KEY,
};

let meaningData = {
    // 'title': 'test json response',
    // 'message': 'this is a message',
    // 'time': 'now'
}

module.exports = meaningData